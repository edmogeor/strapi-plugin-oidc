name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Clear legacy npm config
        run: npm config delete always-auth 2>/dev/null || true

      - name: Install dependencies in root
        run: npm ci || npm install

      - name: Build plugin
        run: npm run build

      - name: Run ESLint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Install dependencies in test-app
        run: |
          cd test-app
          npm ci || npm install
          npm run build

      - name: Run E2E Tests
        run: npm run test

  health-badge:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install fallow
        run: npm install -g fallow

      - name: Generate health badge
        run: |
          mkdir -p badge-output
          fallow health --format badge > badge-output/badge.svg

      - name: Publish badge
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: badge-output
          publish_branch: badges
          force_orphan: true
          enable_jekyll: false
